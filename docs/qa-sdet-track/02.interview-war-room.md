## Security Testing Example

Example negative authentication tests implemented in the project:

1. Invalid JWT token → 401 Unauthorized
2. Expired JWT token → 401 Unauthorized
3. Valid token but wrong role → 403 Forbidden

Purpose:
Verify that the API correctly enforces authentication and authorization rules.s